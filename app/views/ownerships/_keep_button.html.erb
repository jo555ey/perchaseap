<% if current_user.keep?(item) %>
  <%= form_tag(ownership_path(item.id), method: :delete) do %>
    <%= hidden_field_tag :item_id, item.id %>
    <%= hidden_field_tag :type, 'Keep' %>
    <%= submit_tag 'Keep', class: 'btn btn-success' %>
  <% end %>
<% else %>
  <%= form_tag(ownerships_path) do %>
    <%= hidden_field_tag :item_code, item.code %>
    <%= hidden_field_tag :type, 'Keep' %>
    <%= submit_tag 'Keep it', class: 'btn btn-primary' %>
  <% end %>
<% end %>